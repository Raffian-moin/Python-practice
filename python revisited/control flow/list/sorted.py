# do not modify the original list. modify and return

numbers = [2, 5, 4, 9, 7]

sorted_numbers = sorted(numbers)

print(sorted_numbers)

sorted_numbers = sorted(numbers, reverse=True)

print(sorted_numbers)
